name: build

on:
    push:
        branches:
          - '**'

jobs:
    build:

      runs-on: ubuntu-latest
      
      steps:
        
        - uses: actions/checkout@v2
          with:
              persist-credentials: false
              fetch-depth: 0
                
        - run: cd server && yarn && ./test.sh
        - run: cd client && yarn
